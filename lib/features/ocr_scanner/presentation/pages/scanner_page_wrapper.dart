import 'package:flutter/material.dart';
import 'scanner_page.dart';

class ScannerPageWrapper extends StatelessWidget {
  const ScannerPageWrapper({super.key});

  @override
  Widget build(BuildContext context) {
    // Simple wrapper without additional navigation
    return const ScannerPage();
  }
}
